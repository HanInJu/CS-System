<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heather.cs.hello.mapper.HelloMapper">
    <select id="select" resultType="String">
        SELECT 'hello'
          FROM DUAL
    </select>
</mapper>