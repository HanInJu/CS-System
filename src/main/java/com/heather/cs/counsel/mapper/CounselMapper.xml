<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heather.cs.counsel.mapper.CounselMapper">

    <insert id="insertCounsel" parameterType="counsel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO counsel(category_id, charger_id, title, content, customer_name, customer_email, status, creator_id, modifier_id, created_at, modified_at)
        VALUES(#{categoryId}, #{chargerId}, #{title}, #{content}, #{customerName}, #{customerEmail}, #{status}, #{creatorId}, #{modifierId}, now(), now())
    </insert>

<!--    여기도 -->
    <insert id="insertCounselHistory" parameterType="long">
        INSERT INTO counsel_history(counsel_id, sequence, category_id, title, content, requestor_email, charger_id, status, creator_id, created_at)
        SELECT c.id AS counsel_id,
               (COUNT(ch.counsel_id)) + 1 AS sequence,
               c.category_id, c.title, c.content, customer_email AS requestor_email, c.charger_id, c.status, modifier_id, modified_at
        FROM counsel AS c
        LEFT JOIN counsel_history AS ch on c.id = ch.counsel_id
        WHERE c.id = #{counselId};
    </insert>

<!--    카테고리를 왜 조인? 상담에 카테고리 아이디 컬럼이 있는데 굳이...? -->
    <select id="selectCounselsWithoutCounselor" parameterType="long" resultType="counsel">
        SELECT co.id, co.category_id
        FROM counsel co
        INNER JOIN category c on co.category_id = c.id
        WHERE co.category_id = #{categoryId} AND co.charger_id IS NULL;
    </select>

<!--    담당자를 할당시켜준다는 느낌의 메서드 이름이 낫지 않나 싶음 나중에 범용 Counsel이 업데이트되어야 할 경우 메서드명 어떻게 할건지 한 번 더 생각해봐야 함 -->
<!--    여러 개 생기는 게 싫지만 이렇게 너무 범용적인 메서드명은 좀...-->
<!--    처음에는 작은 단위로 하다가 나중에 많아지면 통합하는 식으로 리팩토링하기도 함-->
<!--    update가 작은 단위로 될 경우, update 메서드 여러 개 만드는 편이고, 큰 단위로 업데이트될 경우 (객체로 받을만큼) 범용으로 Counsel 이렇게 쓰는 편-->
    <update id="updateCounsel" parameterType="counsel">
        UPDATE counsel
        SET charger_id = #{chargerId},
            modifier_id = #{modifierId},
            modified_at = now()
        WHERE id = #{id};
    </update>

</mapper>