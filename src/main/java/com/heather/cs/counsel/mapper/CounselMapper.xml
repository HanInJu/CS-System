<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heather.cs.counsel.mapper.CounselMapper">

    <insert id="insertCounsel" parameterType="counsel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO counsel(category_id, title, content, customer_name, customer_email, status, creator_id, modifier_id, created_at, modified_at)
        VALUES(#{categoryId}, #{title}, #{content}, #{customerName}, #{customerEmail}, #{status}, #{creatorId}, #{modifierId}, now(), now())
    </insert>

    <insert id="insertCounselHistory" parameterType="Long">
        INSERT INTO counsel_history(counsel_id, sequence, category_id, title, content, requestor_email, charger_id, status, creator_id, created_at)
        SELECT c.id AS counsel_id,
               (COUNT(ch.counsel_id)) + 1 AS sequence,
               c.category_id, c.title, c.content, customer_email AS requestor_email, c.charger_id, c.status, modifier_id, modified_at
        FROM counsel AS c
        LEFT JOIN counsel_history AS ch on c.id = ch.counsel_id
        WHERE c.id = #{counselId};
    </insert>

</mapper>